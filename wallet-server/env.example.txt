# Application Environment
APP_ENV=local
IS_API=1
IS_WORKER=0
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=wallet_server
DB_SYNC=1
DB_DEBUG=0

# Redis Configuration (for caching and queues)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DATABASE=0
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_FAMILY=0

# Cache Configuration
CACHE_TTL=3600000

# JWT Configuration
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production

# Wallet Security Configuration
# MASTER_KEY: Used to encrypt private keys (AES-256-GCM)
# Use a strong secret string (will be hashed with SHA256)
MASTER_KEY=your-super-secret-master-key-change-this-in-production

# IP Whitelist Configuration
# Comma-separated list of allowed IP addresses
# Use '*' to allow all IPs (NOT RECOMMENDED for production)
# Examples:
#   IP_WHITELIST=127.0.0.1,192.168.1.100,10.0.0.5
#   IP_WHITELIST=*  (allow all - only for development)
IP_WHITELIST=127.0.0.1,::1

# API Key Authentication
# API keys are stored in database (admin_configs table with key='api_keys')
# Generate keys using: cd scripts && pnpm run generate-api-key
# See API_KEY_SETUP.md for detailed setup instructions

# Default API Key (optional)
# If not set, will use: mongker
# For production, generate a secure key: cd scripts && pnpm run generate-api-key
DEFAULT_API_KEY=

